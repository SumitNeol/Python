# all slicing method
a =[]
n=int(input("Enter the number of the elements: "))
for i in range(n):
    a.append(input("Enter the elements of the list: "))

print(a)


#Slicing for a spcific number of elements
a[0:4]

#Slicing for a skipping elements
def

